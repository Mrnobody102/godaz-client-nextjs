// Deprecated: `LoginForm` was replaced by `AuthModal`.
// Kept as a no-op export to avoid breaking imports until the file is fully removed.

export function LoginForm() {
  console.warn('LoginForm is deprecated — use AuthModal instead');
  return null;
}
